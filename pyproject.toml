[tool.poetry]
name = "trabalho-ia"
version = "0.1.0"
description = ""
authors = ["Gregs, Isis"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
